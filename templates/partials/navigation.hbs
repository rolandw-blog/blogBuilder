<nav>
  <div class="nav-section">
    <div>
      created: {{getFirstChange changes}}
    </div>
    <div>
      last edited: {{getLastChange changes}}
    </div>

    <div>
    {{!-- path: <a href="/">~</a>/
      {{~#each pagination ~}}
        <a href="{{getPaginationSeg @index}}">{{this}}</a>/
        {{~^}}
      {{/each}} --}}
    </div>
    <div>
      page source: <a href="{{sourceUrl}}">{{getUrlOrigin sourceUrl }}</a>
    </div>
  </div>
</nav>
